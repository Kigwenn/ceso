{% extends 'base.html.twig' %}

{% block title %}Catalogues{% endblock %}

{% block body %}
    <h1>Catalogues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catalogue.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ catalogue.nom }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ catalogue.duree }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ catalogue.tarif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('catalogues_index') }}">back to list</a>

    <a href="{{ path('catalogues_edit', {'id': catalogue.id}) }}">edit</a>

    {{ include('catalogues/_delete_form.html.twig') }}
{% endblock %}
