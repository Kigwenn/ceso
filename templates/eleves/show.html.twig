{% extends 'base.html.twig' %}

{% block title %}Eleves{% endblock %}

{% block body %}
    <h1>Eleves</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elefe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ elefe.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ elefe.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ elefe.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ elefe.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eleves_index') }}">back to list</a>

    <a href="{{ path('eleves_edit', {'id': elefe.id}) }}">edit</a>

    {{ include('eleves/_delete_form.html.twig') }}
{% endblock %}
