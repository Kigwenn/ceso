{% extends 'base.html.twig' %}

{% block title %}Formations{% endblock %}

{% block body %}
    <h1>Formations</h1>

    <table class="table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Date de debut</th>
                <td>{{ formation.dateDeb ? formation.dateDeb|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nombre de participant</th>
                <td>{{ formation.nbParticipant }}</td>
            </tr>
            <tr>
                <th>Actionns</th>
                <bouton type="bouton" class="btn btn-warning">
                    <a class="btn btn-warning href="{{ path('formations_edit', {'id': formation.id}) }}">edit</a>
                </bouton>Modifier
                <div class="btn btn-default">
                <a href="{{ path('formations_index') }}">Retour à la liste</a>
                </div>
            </tr>
        </tbody>
    </table>
{#<div class="row">#}
    {#<div class="col-lg-offset-1 col-lg-10">#}
        {#<div class="btn btn-default">#}
            {#<a href="{{ path('formations_index') }}">Retour à la liste</a>#}
        {#</div>#}
        {#<bouton type="bouton" class="btn btn-warning">#}
            {#<a class="btn btn-warning href="{{ path('formations_edit', {'id': formation.id}) }}">edit</a>#}
        {#</bouton>Modifier#}
    {#</div>#}
{#</div>#}
    {#<a href="{{ path('formations_index') }}">back to list</a>#}

    {#<a href="{{ path('formations_edit', {'id': formation.id}) }}">edit</a>#}

    {{ include('formations/_delete_form.html.twig') }}
{% endblock %}
